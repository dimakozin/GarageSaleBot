/**
 *   IMPORTANT! 
 *   Every function must have arguments:
 *   @bot - instance of telegram bot
 *   @msg - user message
 * 
 */

export default {
    sendCategories: (bot, msg) => {
        console.log('Work?')
    }
}